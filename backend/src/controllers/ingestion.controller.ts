import type { Request, Response } from "express";

// Validate middleware will run to validate category

export function handleImageUpload(req: Request, res: Response) {
  
}

export function handleTextUpload(req: Request, res: Response) {

}